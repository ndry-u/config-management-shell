# Стартовый скрипт для тестирования Этапа 2.
# Комментарии начинаются с #.

ls
ls -la /tmp
cd "My Documents"
cd 'Program Files'

# Неизвестная команда должна быть пропущена, выполнение не прерывается.
abcd

# Ошибочная строка — незакрытая кавычка; тоже пропускается.
echo "unterminated

ls